name: DEPLOY
on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: rsync deployments
      uses: burnett01/rsync-deployments@5.2.1
      with:
        switches: -avzr --delete
        path: src/
        remote_path: /GameBot
        remote_host: 185.250.249.107
        remote_user: ubuntu
        remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
